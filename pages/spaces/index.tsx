import { Mic } from 'lucide-react'

import Page from '@/components/Page'

export default function Spaces(): JSX.Element {
  return (
    <div className="px-12">
      <Page icon={Mic} title="Spaces">
        ...
      </Page>
    </div>
  )
}
